# Frameworks (frameworks)

Algorithmic drivers that replace problem-specific parts via callbacks. `mk.column_generation` / `mk.price_and_branch` / `mk.benders`.

::: minlpkit.frameworks
    options:
      members:
        - column_generation
        - price_and_branch
        - benders