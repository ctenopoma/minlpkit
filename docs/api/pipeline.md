# パイプライン (analyze / Report)

観測量収集から診断までを model 非依存で一気通貫に行うパイプライン。
`import minlpkit as mk` で `mk.analyze` / `mk.collect_metrics` / `mk.Report` として使える。

::: minlpkit.pipeline
    options:
      members:
        - analyze
        - collect_metrics
        - Report
