# Comparison (compare_variants)

A comparison harness that measures before/after improvements divided into root dual bound (formulation quality) and time-limited gap/nodes (solving performance). Available as `mk.compare_variants`.

::: minlpkit.compare
    options:
      members:
        - compare_variants
        - measure