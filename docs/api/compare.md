# 比較 (compare_variants)

改善の before/after を、ルート双対境界(定式化の質)と時間制限gap/ノード(求解性能)に
分けて測る比較 harness。`mk.compare_variants` として使える。

::: minlpkit.compare
    options:
      members:
        - compare_variants
        - measure
