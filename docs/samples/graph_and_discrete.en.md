# Graphs and Discrete Structures

Discrete optimization of graph/combinatorial structures such as coloring, covering, partitioning, and matching. Subjects for symmetry breaking and set partitioning.

**11 Models** / 1 model supports the `scale` argument. A ✓ in the `scale` column means the scale can be varied with `build_model(scale=...)`.

| Sample | Business Story | scale | Source |
| --- | --- | :---: | :---: |
| feature_selection | Sparse Regression via Feature Selection (MIP) — Feature Selection for Regression — A data scientist in the clinical data analysis team builds a regression model to predict future health risk scores based on test values (10 items such as blood pressure, BMI, and blood sugar level) of medical checkup examinees. | — | [source](https://github.com/ctenopoma/minlpkit/blob/main/samples/graph_and_discrete/feature_selection.py) |
| frequency_assignment | Frequency Assignment Problem (MIP) — Frequency Assignment Problem — A wireless network designer at a mobile telecommunications operator assigns frequency channels to each base station (cell) installed in the city. Assigning the same frequency to base stations that are close to each other and interfere with each other degrades call quality, so the goal is to assign different frequencies to pairs of interfering base stations while minimizing the maximum frequency number used (which is the frequency bandwidth to secure, directly related to license costs). | — | [source](https://github.com/ctenopoma/minlpkit/blob/main/samples/graph_and_discrete/frequency_assignment.py) |
| graph_coloring | Graph Coloring (MILP) — For demonstrating symmetry breaking — Colors adjacent vertices with different colors and minimizes the number of colors used. Since colors are completely interchangeable | — | [source](https://github.com/ctenopoma/minlpkit/blob/main/samples/graph_and_discrete/graph_coloring.py) |
| k_means_mip | K-means Clustering (MIP Formulation) — K-Means Clustering — A logistics planning manager at a retail chain simultaneously determines to which store a newly established distribution center will be attached based on the locations (coordinates) of each store, and to which distribution center's delivery area each store will be assigned. By minimizing the sum of squared distances between stores and distribution centers, the total travel distance and delivery cost of the delivery route are suppressed (a k-medoids type MIP formulation assuming the distribution center is attached to one of the existing stores). | — | [source](https://github.com/ctenopoma/minlpkit/blob/main/samples/graph_and_discrete/k_means_mip.py) |
| max_clique_mip | Maximum Clique Problem (MIP Formulation) — Maximum Clique Problem — A project team formation manager in the HR department finds the largest group (clique) where everyone has a good relationship with each other from a graph representing the "collaborated in past projects (good compatibility)" relationship among employees, and recommends them as candidates for the core team of a new project. | — | [source](https://github.com/ctenopoma/minlpkit/blob/main/samples/graph_and_discrete/max_clique_mip.py) |
| optimal_decision_tree | Optimal Decision Tree (Simplified MIP) — Optimal Decision Tree — A loan screening manager at a consumer finance company learns an interpretable depth-1 decision tree rule with MIP that determines "approve/reject" from the applicant's attributes (3 indicators: annual income score, desired loan amount score, and existing loan count score). By narrowing down to a single branching rule "approve if this indicator exceeds this threshold" instead of a complex black-box model, it minimizes misjudgments on the past 16 screening results while fulfilling accountability to screening personnel and regulatory authorities. | — | [source](https://github.com/ctenopoma/minlpkit/blob/main/samples/graph_and_discrete/optimal_decision_tree.py) |
| qap_linearized | Linearized Quadratic Assignment Problem (QAP) — Linearized Quadratic Assignment Problem — A factory layout designer allocates multiple production facilities (machines/processes) to candidate installation locations in the factory, minimizing the total sum of the product of the physical distance between installation locations and the material flow (transportation cost) going back and forth between facilities. Since it is necessary to consider the flow between facilities and the distance between locations simultaneously, the objective function is inherently quadratic (product of assignment variables), and it is linearized by the Adams-Johnson method and solved as a MIP. | — | [source](https://github.com/ctenopoma/minlpkit/blob/main/samples/graph_and_discrete/qap_linearized.py) |
| set_cover | Set Cover Problem (MIP) — Set Cover Problem — A city's fire department deployment planner selects and opens the minimum number of fire stations from candidate locations (the combinations of districts that can be covered differ for each candidate location) to bring all 15 districts in the city into the emergency response coverage area. The goal is to keep opening and maintenance costs down while ensuring that no district is left uncovered. | — | [source](https://github.com/ctenopoma/minlpkit/blob/main/samples/graph_and_discrete/set_cover.py) |
| set_partitioning | Large-Scale Set Partitioning Problem (MILP) — For verifying GPU primal heuristics — A prototype for crew pairing, etc. Selects a combination of columns that cover the entire set of elements exactly once at the minimum cost. | ✓ | [source](https://github.com/ctenopoma/minlpkit/blob/main/samples/graph_and_discrete/set_partitioning.py) |
| steiner_tree | Steiner Tree Problem (MIP) — Steiner Tree Problem in Graphs — A communications carrier's network installation manager designs an optical fiber network connecting multiple sites ("endpoints" that must be connected, such as data centers and base stations) at the minimum installation cost (distance/construction cost). It may pass through relay nodes other than endpoints, but passing through them does not incur additional costs itself, and only the cost of the actually installed lines (edges) is incurred, so it can be cheaper than a tree connecting only endpoints directly (this is the Steiner tree problem). | — | [source](https://github.com/ctenopoma/minlpkit/blob/main/samples/graph_and_discrete/steiner_tree.py) |
| sudoku_mip | Sudoku Solver (MIP Formulation) — Sudoku Solver — An editor of a puzzle magazine has a puzzle solved by integer programming to verify whether a given 9x9 Sudoku puzzle has a unique solution. Formulated as a pure feasibility problem without an objective function, it finds one solution that satisfies all given hint numbers and Sudoku rules (no duplicate numbers in rows, columns, and 3x3 blocks). | — | [source](https://github.com/ctenopoma/minlpkit/blob/main/samples/graph_and_discrete/sudoku_mip.py) |

[← Back to the Catalog](index.en.md)
